'use client';

import AlbaPage from '@/features/alba/components/AlbaPage';
import InnerContainer from '@/shared/components/container/InnerContainer';

const AlbaDetailPage = () => {
  return (
    <InnerContainer>
      <AlbaPage />
    </InnerContainer>
  );
};

export default AlbaDetailPage;
